/**
 * Brand copy — single source of truth.
 * ====================================
 * Positioning lines live here, not inline in components, so changing the
 * brand line is a one-line edit rather than a search across surfaces.
 *
 * See 08_POSITIONING_AND_BRAND.md for the reasoning behind each. The rules
 * that constrain anything added to this file:
 *
 *   - NEVER "independent", "unconflicted", "unbiased", or any claim that we
 *     earn nothing from what a user buys. Retired 2026-09-17.
 *   - NEVER "best interest", "in your best interest", or other fiduciary
 *     language. An AMFI-registered distributor paid a trail commission does
 *     not owe a fiduciary duty; a SEBI-registered Investment Adviser does.
 *     Borrowing that language is a sharper version of the exact problem the
 *     repositioning exists to fix.
 *   - NEVER "best fund", "recommended", "our picks", "you should".
 *     Descriptive, never prescriptive (01 §6).
 *   - Every claim must trace to a number in the product or a line in the
 *     published methodology. If it cannot be linked, it does not ship.
 */

/** The brand line. Claims alignment and discloses the conflict in one breath —
 *  which is the whole positioning compressed to six words. */
export const TAGLINE = 'In your interest — and open about ours.'

/** Terse variant for the nav and tight spaces. Makes no commission claim, so
 *  it survived the repositioning untouched. */
export const TAGLINE_SHORT = 'data over hype'

/** Hero headline. The question the product exists to answer. */
export const HEADLINE = 'Skill or luck?'

/** One-sentence explanation of what the product does, in plain language. */
export const SUBHEAD =
  'Most of a fund’s return is the market, its style and its sector — not the manager. ' +
  'AlphaPicker decomposes the returns of every Indian mutual fund and tells you how much ' +
  'the manager actually earned.'
